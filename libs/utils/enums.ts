export enum AppEnv {
  PRODUCTION = 'production',
  STAGING = 'staging',
  DEVELOPMENT = 'development',
  TEST = 'test',
}

export enum Status {
  PENDING = 'pending',
  AWAITING = 'awaiting',
  UPCOMING = 'upcoming',
  COMPLETED = 'completed',
  DONE = 'done',
}

export enum Role {
  ADMIN = 'admin',
  USER = 'user',
  COACH = 'coach',
}

export enum Gender {
  MALE = 'male',
  FEMALE = 'female',
}
